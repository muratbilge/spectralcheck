# The app is pure Kotlin with no reflection, JNI callbacks or serialization,
# so the default Android + library-provided keep rules are sufficient.
# Add project-specific keep rules here if that ever changes.
